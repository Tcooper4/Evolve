"""
Reinforcement-learning training helpers — removed (v4.6.2).

The prior RL stack depended on optional third-party packages; that feature
is no longer supported. This module remains as an empty placeholder so
imports resolve without deleting the file.
"""
