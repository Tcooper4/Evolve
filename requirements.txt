# Evolve Trading Platform — single-file requirements
# Install: pip install -r requirements.txt
# For minimal install first: pip install -r requirements_core.txt then -r requirements_optional.txt

# --- Core (required for app to start) ---
streamlit>=1.28.0
PyYAML>=6.0.0
SQLAlchemy>=2.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0

# --- Config & validation ---
python-dotenv>=1.0.0
jsonschema>=4.18.0

# --- Visualization (used by Portfolio, Risk, Performance, Reports, Forecasting, etc.) ---
plotly>=5.15.0

# --- HTTP & data ---
requests>=2.28.0
yfinance>=0.2.28
tenacity>=8.2.0

# --- ML & stats (used by strategies, backtest, models) ---
scikit-learn>=1.3.0
scipy>=1.10.0

# --- Scheduling ---
schedule>=1.2.0

# --- AI/LLM (briefing, Chat) ---
anthropic>=0.7.0

# --- Reporting (PDF export in Reports page) ---
reportlab>=4.0.0

# --- Optional: uncomment for full features ---
# alpha-vantage>=3.0.0
# alpaca-py>=0.15.0
# prophet>=1.1.4
# torch>=2.0.0
# pytorch-lightning>=2.0.0
# openai>=1.0.0
